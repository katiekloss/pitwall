on: [push]
jobs:
  test-watch-py:
    runs-on: docker
    steps:
      - uses: actions/checkout@v4
      - uses: https://github.com/astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: Run watch.py
        run: find data/ -type f | xargs -I % uv run --frozen --no-progress watch.py -i %
